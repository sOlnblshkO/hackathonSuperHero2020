<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.forfedorova">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".representative.repActivitiy"
            android:label="@string/title_activity_rep_activitiy"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".mainFiles.adminRep" />
        <activity android:name=".mainFiles.addVisitorsActivity" />
        <activity android:name=".mainFiles.orgEditActivity" />
        <activity android:name=".mainFiles.createOrgActivity" />
        <activity android:name=".mainFiles.createActivityForOrgActivity" />
        <activity
            android:name=".superAdmin.superAdminActivity"
            android:label="@string/title_activity_super_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".administrator.adminMenuActivity"
            android:label="@string/title_activity_admin_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".schoolkid.ui.schoolKidMenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mainFiles.profilePage" />
        <activity android:name=".LogAndReg.registrActivity" />
        <activity android:name=".LogAndReg.loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>